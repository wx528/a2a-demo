services:
  research-agent:
    build:
      context: .
      dockerfile: research_agent/Dockerfile
    ports:
      - "8001:8001"
    environment:
      - PORT=8001
      - HOST=research-agent
      - LLM_API_KEY=${LLM_API_KEY:-}
      - LLM_BASE_URL=${LLM_BASE_URL:-}
      - LLM_MODEL=${LLM_MODEL:-gpt-4o-mini}
      - TASK_DB=/data/tasks.db
    volumes:
      - research-agent-data:/data
    networks:
      - a2a-network

  writing-agent:
    build:
      context: .
      dockerfile: writing_agent/Dockerfile
    ports:
      - "8002:8002"
    environment:
      - PORT=8002
      - HOST=writing-agent
      - LLM_API_KEY=${LLM_API_KEY:-}
      - LLM_BASE_URL=${LLM_BASE_URL:-}
      - LLM_MODEL=${LLM_MODEL:-gpt-4o-mini}
      - TASK_DB=/data/tasks.db
    volumes:
      - writing-agent-data:/data
    networks:
      - a2a-network

  debate-agent:
    build:
      context: .
      dockerfile: debate_agent/Dockerfile
    ports:
      - "8003:8003"
    environment:
      - PORT=8003
      - HOST=debate-agent
      - LLM_API_KEY=${LLM_API_KEY:-}
      - LLM_BASE_URL=${LLM_BASE_URL:-}
      - LLM_MODEL=${LLM_MODEL:-gpt-4o-mini}
      - SEARCH_PROVIDER=${SEARCH_PROVIDER:-duckduckgo}
      - SEARCH_API_KEY=${SEARCH_API_KEY:-}
      - SEARCH_MAX_RESULTS=${SEARCH_MAX_RESULTS:-5}
      - TASK_DB=/data/tasks.db
    volumes:
      - debate-agent-data:/data
    networks:
      - a2a-network

  orchestrator:
    build:
      context: .
      dockerfile: orchestrator/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - AGENT_URLS=${AGENT_URLS:-http://research-agent:8001,http://writing-agent:8002,http://debate-agent:8003}
      - RESEARCH_AGENT_URL=http://research-agent:8001
      - WRITING_AGENT_URL=http://writing-agent:8002
    depends_on:
      - research-agent
      - writing-agent
      - debate-agent
    networks:
      - a2a-network

  web:
    build:
      context: .
      dockerfile: web/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - RESEARCH_AGENT_URL=http://research-agent:8001
      - WRITING_AGENT_URL=http://writing-agent:8002
      - LLM_API_KEY=${LLM_API_KEY:-}
      - LLM_BASE_URL=${LLM_BASE_URL:-}
      - LLM_MODEL=${LLM_MODEL:-gpt-4o-mini}
    depends_on:
      - research-agent
      - writing-agent
    networks:
      - a2a-network

networks:
  a2a-network:
    driver: bridge

volumes:
  research-agent-data:
  writing-agent-data:
  debate-agent-data:
